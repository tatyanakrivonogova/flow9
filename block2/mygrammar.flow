export {
    Expr ::= Int, Var, Add, Mul, Sub, Div, Neg, Pow;
    Int : (value : int);
    Var : (name: string);
    Mul : (l : Expr, r : Expr);
    Add : (l : Expr, r : Expr);
    Sub : (l : Expr, r : Expr);
    Div : (l : Expr, r : Expr);
    Neg : (e: Expr);
    Pow : (l : Expr, r : Expr);
}